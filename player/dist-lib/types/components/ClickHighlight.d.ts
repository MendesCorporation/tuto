interface Props {
    x: number;
    y: number;
    viewportWidth: number;
    viewportHeight: number;
}
export default function ClickHighlight({ x, y, viewportWidth, viewportHeight }: Props): import("react/jsx-runtime").JSX.Element;
export {};
